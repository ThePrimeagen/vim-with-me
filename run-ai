#!/usr/bin/env bash

for i in {0..49}; do
    echo "running $1 at round $i"
    rm /tmp/td && LEVEL=error go run ./examples/v2/td/run/main.go --system $1 --seed $i --debug /tmp/td -- ./zig-out/bin/to main.json >> $1_results_strat
done

